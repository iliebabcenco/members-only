<%= form_with model: @post do |f| %>

<%= f.text_field :title, required: true, placeholder: "Introduce the title" %>
<%= f.text_field :body, required: true, placeholder: "Introduce the content..." %>
<%= f.button :submit %>

<% end %>